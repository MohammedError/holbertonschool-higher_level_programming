The ``101-lazy_matrix_mul`` module
==================================

Using ``lazy_matrix_mul``
-------------------------

Importing the function:
    >>> lazy_matrix_mul = __import__('101-lazy_matrix_mul').lazy_matrix_mul

Basic Multiplication (2x2 * 2x2):
    >>> print(lazy_matrix_mul([[1, 2], [3, 4]], [[1, 2], [3, 4]]))
    [[ 7 10]
     [15 22]]

Vector Multiplication (1x2 * 2x2):
    >>> print(lazy_matrix_mul([[1, 2]], [[3, 4], [5, 6]]))
    [[13 16]]

Different Dimensions (2x3 * 3x2):
    >>> a = [[1, 2, 3], [4, 5, 6]]
    >>> b = [[1, 2], [3, 4], [5, 6]]
    >>> print(lazy_matrix_mul(a, b))
    [[22 28]
     [49 64]]

Floats:
    >>> print(lazy_matrix_mul([[1.5, 2.0], [3.0, 4.5]], [[1.0, 2.0], [3.0, 4.0]]))
    [[ 7.5 11. ]
     [16.5 24. ]]

--- Validations (NumPy Handling) ---

Invalid dimensions (Incompatible shapes):
    >>> try:
    ...     lazy_matrix_mul([[1, 2]], [[1, 2]])
    ... except Exception as e:
    ...     print(e)
    ...
    shapes (1,2) and (1,2) not aligned: 2 (dim 1) != 1 (dim 0)

Not a list (Scalar vs Matrix):
    >>> try:
    ...     lazy_matrix_mul("Not a list", [[1, 2]])
    ... except Exception as e:
    ...     print(type(e).__name__)
    ...
    ValueError

Empty Matrix:
    >>> try:
    ...     lazy_matrix_mul([], [[1, 2]])
    ... except Exception as e:
    ...     print(e)
    ...
    shapes (0,) and (1,2) not aligned: 0 (dim 0) != 1 (dim 0)
