The ``101-lazy_matrix_mul`` module
==================================

Using ``lazy_matrix_mul``
-------------------------

Importing the function:
    >>> lazy_matrix_mul = __import__('101-lazy_matrix_mul').lazy_matrix_mul

Basic Multiplication:
    >>> print(lazy_matrix_mul([[1, 2], [3, 4]], [[1, 2], [3, 4]]))
    [[ 7 10]
     [15 22]]

    >>> print(lazy_matrix_mul([[1, 2]], [[3, 4], [5, 6]]))
    [[13 16]]

--- Edge Cases and Exceptions ---

Test present: ma = String
    >>> try:
    ...     lazy_matrix_mul("Holberton", [[5, 6], [7, 8]])
    ... except Exception as e:
    ...     print(e)
    Scalar operands are not allowed, use '*' instead

Test present: mb = String
    >>> try:
    ...     lazy_matrix_mul([[5, 6], [7, 8]], "Holberton")
    ... except Exception as e:
    ...     print(e)
    Scalar operands are not allowed, use '*' instead

Test present: ma = Scalar (int)
    >>> try:
    ...     lazy_matrix_mul(1, [[5, 6], [7, 8]])
    ... except Exception as e:
    ...     print(e)
    Scalar operands are not allowed, use '*' instead

Test present: ma = Empty list
    >>> try:
    ...     lazy_matrix_mul([], [[5, 6], [7, 8]])
    ... except Exception as e:
    ...     print(e)
    shapes (0,) and (2,2) not aligned: 0 (dim 0) != 2 (dim 0)

Test present: ma = Empty list of lists
    >>> try:
    ...     lazy_matrix_mul([[]], [[5, 6], [7, 8]])
    ... except Exception as e:
    ...     print(e)
    shapes (1,0) and (2,2) not aligned: 0 (dim 1) != 2 (dim 0)

Test present: mb = Empty list
    >>> try:
    ...     lazy_matrix_mul([[5, 6], [7, 8]], [])
    ... except Exception as e:
    ...     print(e)
    shapes (2,2) and (0,) not aligned: 2 (dim 1) != 0 (dim 0)

Test present: ma = List of lists of not number (Mixed types/Strings)
    >>> try:
    ...     lazy_matrix_mul([[1, "2"], [3, 4]], [[1, 2], [3, 4]])
    ... except Exception as e:
    ...     print(e)
    ...

Test present: mb = List of lists of not number
    >>> try:
    ...     lazy_matrix_mul([[1, 2], [3, 4]], [[1, "2"], [3, 4]])
    ... except Exception as e:
    ...     print(e)
    ...

Test present: ma = List of lists of number various size (Jagged)
    >>> try:
    ...     lazy_matrix_mul([[1, 2, 3], [4, 5]], [[1, 2], [3, 4]])
    ... except Exception as e:
    ...     print(e)
    ...

Test present: mb = List of lists of number various size (Jagged)
    >>> try:
    ...     lazy_matrix_mul([[1, 2], [3, 4]], [[1, 2, 3], [4, 5]])
    ... except Exception as e:
    ...     print(e)
    ...

Test present: Incompatible dimensions
    >>> try:
    ...     lazy_matrix_mul([[1, 2], [3, 4]], [[1, 2]])
    ... except Exception as e:
    ...     print(e)
    shapes (2,2) and (1,2) not aligned: 2 (dim 1) != 1 (dim 0)

Test present: missing one argument
    >>> try:
    ...     lazy_matrix_mul([[1, 2]])
    ... except Exception as e:
    ...     print(e)
    lazy_matrix_mul() missing 1 required positional argument: 'm_b'

Test present: missing two arguments
    >>> try:
    ...     lazy_matrix_mul()
    ... except Exception as e:
    ...     print(e)
    lazy_matrix_mul() missing 2 required positional arguments: 'm_a' and 'm_b'
